import os

env_file_path = '/home/jacrawford/my_project/Calculator-Midterm_Project01/.env'

# Only include key-value pairs for environment variables
corrected_env_content = """CALCULATOR_MAX_HISTORY_SIZE=1000
CALCULATORAUTO_SAVE=true
CALCULATOR_PRECISION=10
CALCULATOR_MAX_INPUT_VALUE=1e999
DATABASE_URL="your_sql_connection_string"
"""

# Write the corrected content to the .env file
with open(env_file_path, 'w') as f:
    f.write(corrected_env_content.strip())

print(f"Corrected content written to {env_file_path}")

# Optional: Read and print the corrected content to verify
if os.path.exists(env_file_path):
    with open(env_file_path, 'r') as f:
        env_content = f.read()
        print("\nContent of .env file after correction:")
        print(env_content)
else:
    print(".env file not found at:", env_file_path)tenv import load_dotenv
import os

# Load environment variables from a .env file if it exists
load_dotenv()

# Access and print the value of one of the environment variables
max_history_size = os.getenv('CALCULATOR_MAX_HISTORY_SIZE')

print(f"The value of CALCULATOR_MAX_HISTORY_SIZE is: {max_history_size}")

# Accessing environment variables:
base_dir = os.getenv('CALCULATOR_BASE_DIR')
max_history = os.getenv('CALCULATOR_MAX_HISTORY_SIZE')

# ... and so on for other variables

environment_variables = [
CALCULATOR_BASE_DIR=
CALCULATOR_MAX_HISTORY_SIZE=1000
CALCULATORAUTO_SAVE=true
CALCULATOR_PRECISION=10
CALCULATOR_MAX_INPUT_VALUE=1e999
]
